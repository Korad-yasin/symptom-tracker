// src/navigation/index.tsx
import React from 'react';
import Auth from './Auth'; // Import your Auth stack

const RootNavigator = () => {
  return <Auth />; // For now, we are rendering Auth as default
};

export default RootNavigator;
